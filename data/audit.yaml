# ========================================================================
# 审批流配置 - 资产推送审批
# ========================================================================

asset_push:
  audit_msg:
    title: "资产推送申请"
    msgs:
      - "资产推送申请"
  select_node_user:
    - node_sort: "0"
      user_id: "60"
