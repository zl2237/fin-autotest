# ========================================================================
# 审批流配置 - 资产推送审批
# ========================================================================

submit_action: submit

# 通用查询默认值
query_active_tab: "examine_send"
query_audit_status_pending: "1"
query_expedite_status: []
query_page_no: 1
query_page_size: 20

asset_push:
  audit_msg:
    title: "资产推送申请"
    msgs:
      - "资产推送申请"
  select_node_user:
    - node_sort: "0"
      user_id: "60"

# ========================================================================
# 审批流配置 - 订单锁定审批
# ========================================================================

actual_cost_lock:
  audit_msg:
    title: "业务订单ID"
    msgs:
      - "订单锁定"
  select_node_user:
    - node_sort: "0"
      user_id: "60"
  box_no_continue: false
  check_self_fee_status: false

# ========================================================================
# 审批流配置 - 未放款开票申请审批
# ========================================================================

add_loan_before_invoice:
  audit_msg:
    title: "业务订单ID"
    msgs:
      - "未放款开票申请"
  select_node_user:
    - node_sort: "0"
      user_id: "60"

# ========================================================================
# 审批流配置 - 供应商垫付申请审批
# ========================================================================

add_special_payment_flag:
  audit_msg:
    title: "业务订单ID"
    msgs:
      - "供应商垫付申请"
  select_node_user:
    - node_sort: "0"
      user_id: "60"
